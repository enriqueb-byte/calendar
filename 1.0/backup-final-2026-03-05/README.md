# Final backup — 2026-03-05

Snapshot before moving `calendar-planner.js` into archive.

**Contents:**
- **Split build (current):** `app.js`, `planner.js`, `audit.js`, `perspective.js`
- **Single-file build:** `calendar-planner.js`
- **UI:** `index.html` (loads split scripts)

To run the single-file version, point `index.html` at `calendar-planner.js` instead of the four split scripts.
